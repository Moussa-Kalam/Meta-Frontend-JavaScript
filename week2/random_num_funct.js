/* 
A function that generates a random number between 0 and 45 
and rounds that number
*/

function randomNumber () {
    var num = Math.random() * 45;
    console.log(Math.ceil(num));
}

// randomNumber();


